# 安全与隐私

[English](SECURITY.md) · **简体中文**

Resume Jobs 会处理敏感的本地信息。请把每一条候选人事实、简历、回答、截图、
浏览器配置文件和申请材料包都当作隐私数据对待。

## 漏洞报告

请通过 [GitHub Security Advisories](../../security/advisories/new)（私密披露）
报告你怀疑存在的漏洞；如果只是严重程度较低的加固建议，也可以直接提一个普通
issue。报告中不要包含任何真实的候选人数据。

## 默认安全边界

- 控制台（Dashboard）只监听 `127.0.0.1`。
- 工作流状态由当前的领域记录推导得出，而不是来自浏览器缓存。
- 最终提交由人工完成。
- 登录、CAPTCHA、MFA 和各类验证环节都不做自动化。
- 附加简历需要一次独立的显式操作。
- 敏感问题和高风险回答需要用户确认。
- 模型输出无法改变确定性评分、审批结论、申请状态、浏览器策略、上传策略或
  提交策略。
- 正式发布的 UI 不使用浏览器原生的 `alert`、`confirm`、`prompt` 对话框。
- 控制台的响应会阻止页面被嵌套框架、阻止 MIME 嗅探，并避免泄露 referrer。

## 本地 API 与扩展边界

带有 `Origin` 的浏览器写操作请求，只有来自完全一致的本地回环控制台端口，或者
来自语法合法的 Chrome 扩展来源时才会被接受。其他浏览器来源（包括 localhost 上
的另一个端口）会在进入路由处理之前就被拒绝。不带 `Origin` 的本地 CLI 客户端
仍然处在“本机用户”这一信任边界之内，因此控制台必须始终只绑定 `127.0.0.1`。

本地 API 只校验 Chrome 扩展来源的格式，不会固定绑定某一个永久的扩展 ID，因为
未打包扩展的 ID 会随本地安装环境而变化。即便如此，另一个本地安装的扩展要想拿到
一次交接，仍然需要显式获得回环主机权限、存在一个由用户主动创建的活跃申请会话，
并且拿到完全一致的申请页面 URL。请把已安装的浏览器扩展视为本地浏览器信任边界的
一部分，并卸载你不信任的扩展。未来的签名分发版本可能会加入显式的扩展 ID 配对。

私有扩展交接需要同时满足以下全部条件：

- 请求来源匹配 `chrome-extension://<extension-id>`；
- 存在一个由显式点击 **Start AI Fill Assistant**（启动 AI 填写助手）创建的活跃
  控制台申请会话；
- 当前页面与职位页面经规范化后完全一致——规范化会保留具有功能意义的查询参数，
  同时移除跟踪参数；
- 已经存在一个经过复核的申请材料包。

该交接是只读的，不会返回任何简历文件字节，并且强制 `final_submit_allowed=false`。
控制台只接受来自当前活跃职位/会话的扩展填写报告，并会丢弃其中声称的任何提交结果。

扩展的诊断信息使用同一套扩展来源边界。心跳数据只携带扩展版本、规范化后的当前
URL、页面就绪状态，以及由服务端生成的申请/职位/会话标识符，绝不携带候选人字段值
或简历字节。Native Messaging 不属于本产品的一部分，无需安装任何本地宿主程序或
写入注册表。

在交接缺失或失败之后，扩展不会退回到用缓存的会话去填写。当控制台的 reset epoch
（重置纪元）发生变化时，弹窗的下一次连接会先清除缓存的会话、报告、问题、映射
以及遗留的私有状态，然后再去请求一次新的交接。

扩展不会把私有档案文件声明为 web accessible resources 暴露出去。

它的内容脚本会在公开的 https 页面上运行，因此支持公司自建的招聘域名，而不只是几家
大型 ATS 厂商——但在存在填写会话之前，它始终保持沉默。在任何页面信息离开标签页之前，
扩展会先向本机 app 询问哪些主机属于**活跃**的填写会话；这个询问本身不携带任何页面
数据，其结果会被缓存。只有当页面所属主机在这份名单上时，它的 URL 才会被发送给本机
app。因此，你的日常浏览不会被传输、记录或存储到任何地方。

常规的申请执行器运行过程中，绝不会传输或附加简历字节。附加简历是一个独立的手动
操作，并由产品自身的确认流程把关，它永远不会成为自动字段填写的一部分。

## 公开 URL 与服务商访问

导入一个公开职位 URL 需要经过产品自身的确认。远程 URL 必须使用 HTTPS，且不得
包含凭据，不得指向私有地址、链路本地地址、保留地址、映射后的私有地址、NAT64 或
6to4 的 DNS 目标，不得使用非标准的远程端口，不得存在重定向，也不得返回过大的
响应或非 HTML 响应。

AI 服务商的接入点会被校验：本地服务商必须使用回环地址，远程服务商必须使用
HTTPS。凭据只保存在被 Git 忽略的本地状态中，Settings API 永远不会把它们返回，
也永远不会被写入报告或扩展打包产物。

本地 JSON 持久化采用 fail-closed（失败即拒绝）策略：某个可选存储文件缺失时可以
使用它有文档记录的默认值，但损坏的、无法读取的或权限被拒绝的数据，绝不会被当作
“空但有效”的状态处理。此时原文件会被原样保留，控制台会引导用户去恢复备份，或者
显式重置本地数据。

**Reset Local Data**（重置本地数据）只会删除产品自有的存储和本地浏览器状态。
如果你显式配置了位于这些目录之外的候选人档案文件，它会被保留下来；确有需要时，
请单独删除或替换这个由用户自行管理的文件。

## 绝不要提交到仓库

- `.env` 文件、API key、token、cookie、密码或各类凭据
- 运行时产生的 `data/*.json`（有文档记录的合成模板除外）
- `applications/`、`reports/`、`output/`、`tmp/` 或 `archive/`
- `documents/` 或任何个人简历
- `browser_profiles/` 或 `browser_sessions/`
- `extensions/application_assistant/profile.local.json`
- `extensions/application_assistant/package_bundle.local.js`
- 包含候选人数据或申请数据的截图和录屏

## 在真实站点上使用

在真实站点上填写表单必须有人全程监督，并且只能针对用户选定的那一个职位。继续
之前，请先确认 URL、所选的简历版本、候选人事实、拟定的回答、敏感问题、完成度
预估以及当前的申请状态。

不要绕过站点的防护机制，不要编造候选人事实，也不要用 Resume Jobs 提交虚假信息。
访问真实招聘站点、登录、上传简历或提交申请，都需要用户的显式授权。

## 漏洞报告

不要在公开 issue 中包含个人数据、凭据、私有 URL 或未脱敏的申请内容。请通过私密
渠道，把可复现问题所需的最小技术细节报告给维护者。
