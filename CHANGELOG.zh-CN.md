# 更新日志

[English](CHANGELOG.md) · **简体中文**

本文件记录所有值得注意的产品变更。运行时数据、私有职业档案的改动，以及自动
生成的申请报告不在记录范围内。

## 未发布

### 数据安全

- **“删除全部用户数据”现在会先归档再删除。** `data/` 下的每一个数据存储都会
  先复制到 `archive/`，接口返回中会列出这些副本（`pre_wipe_backups`），让产品
  中破坏性最强的操作变得可恢复。此前一次点击就可能抹掉已审核通过的职业档案，
  且不会在旁边留下任何可用的备份。

### 职位申请

- 多步骤申请表单（跨多个 URL 的向导式门户）现已支持全流程辅助：每进入一个新
  步骤都会被识别、等待页面稳定后填写一次，申请助手在各步骤之间保持绑定。超出
  本次申请范围的页面仍会被拒绝处理。
- 申请助手会在任意公开的申请页面上显示，页面侧的浮标提供一键 **填写这一步**
  和 **重新扫描**。只有存在活动填写会话的主机，其页面 URL 才会被发送到本
  地应用。
- 从已确认链接发生的跳转（招聘网站链接落到公司自有的招聘域名）会被当作同一次
  申请继续跟进，而不再被报告为“已离开申请页面”。

### 简历与求职信

- 岗位定制简历的个人总结始终紧扣目标职位：忽略职位描述的 AI 输出会被拒绝并重
  新采样，所有兜底方案也都会点明目标岗位名称。即使职业档案信息稀少，也一定会
  生成总结段落。
- 求职信在接口契约上保证完整篇幅（2-4 段接地内容，即每句话都能追溯到用户确认
  过的事实），模型可以拿到雇主与岗位信息；当职业档案过于单薄、无法为 AI 求职
  信提供事实支撑时，会立即返回而不再重试。
- 简历解析支持职位名在前的美式排版、带后缀的章节标题
  （`PRODUCTION EXPERIENCE`）、成组罗列的技能行以及模板占位符。
- **求职方向** 可在“我的资料”中编辑，并会同时用于简历总结、求职信和基于职业
  档案的职位搜索。
- 专业到岗位的映射覆盖理科、人文、医疗健康、艺术和运营等 40 多个专业方向，不
  再一律默认映射到工程类岗位。

### 防编造加固

- 修复了对抗性评审中发现的多处接地校验绕过：数字的子串匹配、全角数字、未校验
  的中日韩文本、靠比例稀释混入的杜撰从句、带标点的专有名词，以及引用了既定事
  实清单之外的内容。AI 输出可以提及目标职位，但不得声称求职者曾在该公司任职。

## 1.0.0-rc.1 — 2026-08-12

### 产品功能

- 新增带版本管理的简历智能分析（Resume Intelligence）与职业大脑审核/确认流程。
- 新增可解释的国内/海外职位发现规划，以及新鲜度记忆、生命周期抑制、单公司数量
  上限和来源/岗位/地点的多样性控制。
- 新增确定性匹配加语义匹配（仅作参考建议）的双层机制：用户可在仅提示警告的情
  况下强制覆盖，同时保留不可绕过的安全门禁。
- 新增申请材料包 2.0，包含简历/职业档案绑定、完成度预估、求职信状态、面试准
  备、STAR 案例、能力缺口和风险提示。
- 将 Chrome 扩展与本地浏览器助手统一到同一套申请执行器和申请会话契约之下。
- 新增填写完成后的显式学习候选项、带版本管理的答案记忆，以及不保存具体取值的
  表单字段记忆。

### 稳定性与交互体验

- 使用产品自有的模态框、轻提示、横幅和内联错误提示，替换浏览器原生弹窗。
- 在各类操作、页面刷新和实时更新过程中，保持导航位置、当前选中的申请、材料包
  面板、筛选条件、排序、焦点和滚动位置不变。
- 新增对中断执行和历史遗留执行的恢复能力，以及安全的填写准备流程重启。
- 新增浏览器助手截图脱敏、重试、重新扫描、有边界的关闭流程和崩溃恢复。
- 新增普通版和开发者版 Windows 启动器，支持通过本地回环接口优雅关闭。
- 新增响应式/无障碍、生命周期、重复稳定性、发布隐私和持续浸泡（soak）验证。
- 修复“重置本地数据”会删除用户已配置的外部职业档案的问题。
- 阻止过期或跨职位复用的扩展会话；在移除跟踪参数的同时，保留 ATS 查询中起功能
  作用的标识参数。

### 安全

- 最终提交、登录、验证码/多因素认证、敏感问题与 EEO 问题作答、简历上传，仍然
  全部由用户手动完成。
- 运行时 JSON、简历、申请记录、报告、浏览器状态、凭据和私有备份均不会进入
  GitHub 发布代码树。
- 发布前审计现已覆盖 Unicode 文件名，并拦截编译产物和运行时缓存。
